# Agent reasoning (day 6+). Eval runs offline from the decision cache without this.
ANTHROPIC_API_KEY=

# Optional: Razorpay test-mode adapter demo (day 9). Never used by the eval path.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
