node_modules/
.env
.env.*
!.env.example
*.log
.DS_Store
.cache/llm/*.tmp

# Regenerable from seed via `npm run gen -- --size 2000 --name stress`; too large to commit.
data/batches/stress.*

# Ad-hoc run directories. The frozen submission run is committed explicitly.
results/*/
