node_modules/
.env
.env.*
!.env.example
*.log
.DS_Store
.cache/llm/*.tmp

# Regenerable from seed via `npm run gen -- --size 2000 --name stress`; too large to commit.
data/batches/stress.*

# Ad-hoc run directories. Checkpoint and submission runs are committed explicitly
# with `git add -f` (metrics and per-case results only; the audit ledgers are
# tens of megabytes per run and regenerate byte-identically from the seed).
results/*/
